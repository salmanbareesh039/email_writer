export interface LinkedInProfile {
  name: string;
  jobTitle: string;
  company: string;
  recentActivities: string;
  interests: string;
  achievements: string;
}

export interface EmailTemplate {
  preview: string;
  body: string;
}